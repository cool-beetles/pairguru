.row
  %h1 Most active users from last 7 days:
  %table.table.table-striped
    %thead
      %tr
        %th #
        %th Name
        %th Numbers of comments
    %tbody
    - @top_10.each_with_index do |user, index|
      %tr
        %td= index + 1
        %td= user["name"]
        %td= user["comments_count"]
